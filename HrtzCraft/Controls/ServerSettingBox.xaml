<UserControl x:Name="UserControlServerSettingBox" x:Class="HrtzCraft.Controls.ServerSettingBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="LayoutRoot" DataContext="{Binding ElementName=UserControlServerSettingBox}">
        <StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                <TextBlock Text="DisplayName:" Width="100" VerticalAlignment="Center" Foreground="White" />
                <TextBox Text="{Binding Path=DisplayName}" Width="100"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                <TextBlock Text="Id:" Width="100" VerticalAlignment="Center" Foreground="White" />
                <TextBox Text="{Binding Path=Id}" Width="100"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
